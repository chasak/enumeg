// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"example.com/enumeg/ent/group"
	"example.com/enumeg/ent/video"
)

// Video is the model entity for the Video schema.
type Video struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Videotype holds the value of the "videotype" field.
	Videotype video.Videotype `json:"videotype,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VideoQuery when eager-loading is set.
	Edges        VideoEdges `json:"edges"`
	group_videos *int
}

// VideoEdges holds the relations/edges for other nodes in the graph.
type VideoEdges struct {
	// Group holds the value of the group edge.
	Group *Group `json:"group,omitempty"`
	// Streamers holds the value of the streamers edge.
	Streamers []*User `json:"streamers,omitempty"`
	// Moderaters holds the value of the moderaters edge.
	Moderaters []*User `json:"moderaters,omitempty"`
	// Likes holds the value of the likes edge.
	Likes []*User `json:"likes,omitempty"`
	// Viewers holds the value of the viewers edge.
	Viewers []*User `json:"viewers,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [5]bool
}

// GroupOrErr returns the Group value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VideoEdges) GroupOrErr() (*Group, error) {
	if e.loadedTypes[0] {
		if e.Group == nil {
			// The edge group was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: group.Label}
		}
		return e.Group, nil
	}
	return nil, &NotLoadedError{edge: "group"}
}

// StreamersOrErr returns the Streamers value or an error if the edge
// was not loaded in eager-loading.
func (e VideoEdges) StreamersOrErr() ([]*User, error) {
	if e.loadedTypes[1] {
		return e.Streamers, nil
	}
	return nil, &NotLoadedError{edge: "streamers"}
}

// ModeratersOrErr returns the Moderaters value or an error if the edge
// was not loaded in eager-loading.
func (e VideoEdges) ModeratersOrErr() ([]*User, error) {
	if e.loadedTypes[2] {
		return e.Moderaters, nil
	}
	return nil, &NotLoadedError{edge: "moderaters"}
}

// LikesOrErr returns the Likes value or an error if the edge
// was not loaded in eager-loading.
func (e VideoEdges) LikesOrErr() ([]*User, error) {
	if e.loadedTypes[3] {
		return e.Likes, nil
	}
	return nil, &NotLoadedError{edge: "likes"}
}

// ViewersOrErr returns the Viewers value or an error if the edge
// was not loaded in eager-loading.
func (e VideoEdges) ViewersOrErr() ([]*User, error) {
	if e.loadedTypes[4] {
		return e.Viewers, nil
	}
	return nil, &NotLoadedError{edge: "viewers"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Video) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case video.FieldID:
			values[i] = new(sql.NullInt64)
		case video.FieldTitle, video.FieldVideotype:
			values[i] = new(sql.NullString)
		case video.ForeignKeys[0]: // group_videos
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Video", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Video fields.
func (v *Video) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case video.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			v.ID = int(value.Int64)
		case video.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				v.Title = value.String
			}
		case video.FieldVideotype:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field videotype", values[i])
			} else if value.Valid {
				v.Videotype = video.Videotype(value.String)
			}
		case video.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field group_videos", value)
			} else if value.Valid {
				v.group_videos = new(int)
				*v.group_videos = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryGroup queries the "group" edge of the Video entity.
func (v *Video) QueryGroup() *GroupQuery {
	return (&VideoClient{config: v.config}).QueryGroup(v)
}

// QueryStreamers queries the "streamers" edge of the Video entity.
func (v *Video) QueryStreamers() *UserQuery {
	return (&VideoClient{config: v.config}).QueryStreamers(v)
}

// QueryModeraters queries the "moderaters" edge of the Video entity.
func (v *Video) QueryModeraters() *UserQuery {
	return (&VideoClient{config: v.config}).QueryModeraters(v)
}

// QueryLikes queries the "likes" edge of the Video entity.
func (v *Video) QueryLikes() *UserQuery {
	return (&VideoClient{config: v.config}).QueryLikes(v)
}

// QueryViewers queries the "viewers" edge of the Video entity.
func (v *Video) QueryViewers() *UserQuery {
	return (&VideoClient{config: v.config}).QueryViewers(v)
}

// Update returns a builder for updating this Video.
// Note that you need to call Video.Unwrap() before calling this method if this Video
// was returned from a transaction, and the transaction was committed or rolled back.
func (v *Video) Update() *VideoUpdateOne {
	return (&VideoClient{config: v.config}).UpdateOne(v)
}

// Unwrap unwraps the Video entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (v *Video) Unwrap() *Video {
	tx, ok := v.config.driver.(*txDriver)
	if !ok {
		panic("ent: Video is not a transactional entity")
	}
	v.config.driver = tx.drv
	return v
}

// String implements the fmt.Stringer.
func (v *Video) String() string {
	var builder strings.Builder
	builder.WriteString("Video(")
	builder.WriteString(fmt.Sprintf("id=%v", v.ID))
	builder.WriteString(", title=")
	builder.WriteString(v.Title)
	builder.WriteString(", videotype=")
	builder.WriteString(fmt.Sprintf("%v", v.Videotype))
	builder.WriteByte(')')
	return builder.String()
}

// Videos is a parsable slice of Video.
type Videos []*Video

func (v Videos) config(cfg config) {
	for _i := range v {
		v[_i].config = cfg
	}
}
